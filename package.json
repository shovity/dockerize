{
  "name": "dockerize",
  "version": "1.0.0",
  "description": "is pinwheel galaxy",
  "private": true,
  "scripts": {
    "dev": "docker-compose -f docker-compose.yaml -f docker-compose.dev.yaml up",
    "clean": "docker-compose -f docker-compose.yaml -f docker-compose.dev.yaml down"
  }
}
